{
  "name": "@ai-sprints/ai-worker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "echo lint not configured yet",
    "test": "echo test not configured yet"
  },
  "dependencies": {
    "@ai-sprints/shared-types": "0.1.0",
    "@google/generative-ai": "^0.24.1",
    "openai": "^4.67.0"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.8.3"
  }
}
